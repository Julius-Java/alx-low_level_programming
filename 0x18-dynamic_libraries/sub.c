#include "main.h"
/**
 *sub - function returns the result of the substraction of two numbers
 *@a: left operand
 *@b: right operand
 *Return: the result of the substraction of a and b
 */
int sub(int a, int b)
{
	return (a - b);
}
