#include "main.h"
/**
 *mul - function returns the result of the multiplication of a and b
 *@a: left operand
 *@b: right operand
 *Return: the result of a * b
 */
int mul(int a, int b)
{
	return (a * b);
}
