#include "main.h"
/**
 *div- calculates and returns the division of two numbers a and b
 *@a: left operand
 *@b: right operand
 *Return: the result of the division of a and b
 */
int div_(int a, int b)
{
	return (a / b);
}
