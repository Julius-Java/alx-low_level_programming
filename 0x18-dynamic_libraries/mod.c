#include "main.h"
/**
 *mod - calculates the modulos of two numbers
 *@a: left operand
 *@b: right operand
 *Return: returns the result of the modulos of two numbers
 */
int mod(int a, int b)
{
	return (a % b);
}
