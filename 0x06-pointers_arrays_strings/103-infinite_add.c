#include "main.h"
/**
 * infinite_add - adds two numbers
 * @n1: number to be computed
 * @n2: number to be computed
 * @r: buffer to store the result
 * @size_r: buffer size
 * Return: char
*/
char *infinite_add(char *n1, char *n2, char *r, int size_r)
{
	return (n1 + n2);
}
